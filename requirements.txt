# AudioLDM2 training / VAE reconstruction environment
# Requires: Python 3.10 (create via: conda create -n audioldm_env python=3.10)
# If you need GPU acceleration and have CUDA 11.8, you can optionally install torch/torchaudio/torchvision first from the PyTorch index.
# Otherwise these wheels will auto-resolve from PyPI (may be CPU-only depending on platform).

torch==2.2.1
torchaudio==2.2.1
torchvision==0.17.1
transformers==4.30.2
diffusers==0.25.0
accelerate==0.23.0
huggingface-hub==0.23.2  # pinned after compatibility tests with diffusers 0.25.0
ruamel-yaml==0.18.5
wget==3.2
kornia==0.7.0
braceexpand==0.1.7
webdataset==0.2.75
wandb==0.16.0
pytorch-lightning==2.1.1
einops==0.7.0
h5py==3.10.0
ftfy==6.1.1
pandas==2.1.3
matplotlib==3.8.1
taming-transformers-rom1504==0.0.6
ipdb==0.13.13
soundfile
librosa==0.9.2
scipy
numpy<2  # keep for compatibility with some audio libs
tqdm
sentencepiece
safetensors
cffi  # for soundfile (_cffi_backend)

# Git dependencies
git+https://github.com/haoheliu/passt_hear21.git
# git+https://github.com/haoheliu/audioldm_eval.git
# 上面依赖会通过 ssr_eval -> wave -> MySQL-python 触发 Windows 下 MySQL-python (已弃用) 构建失败。
# 如果后续需要自动评测指标，再安装 Visual C++ Build Tools 或寻找替代评测包后单独安装。
